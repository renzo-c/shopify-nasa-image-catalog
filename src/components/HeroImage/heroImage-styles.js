import { makeStyles } from '@mui/styles';

const useStyles = makeStyles({
  root: {
      margin: '2em 0'
  },
  imgContainer: {
    width: '500px',
    margin: 'auto'
  },
  img: {
    borderRadius: '15px',
    width: '100%',
    height: '100%'
  }
});

export default useStyles;
