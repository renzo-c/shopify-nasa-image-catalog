import { makeStyles } from '@mui/styles';

const useStyles = makeStyles({
  root: {
      boxShadow: "0px 5px 5px -3px rgb(120 191 255 / 20%), 0px 8px 10px 1px rgb(120 191 255 / 14%), 0px 3px 14px 2px rgb(120 191 255 / 12%) !important"
  }
});

export const styles = {
  cardMUI: {
    borderRadius: '15px',
    // margin: '2em 0'
  }
};

export default useStyles;
